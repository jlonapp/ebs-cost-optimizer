# EBS Right-Sizer dependencies
#
# Required:
#   boto3 — AWS SDK
#
# Optional:
#   openpyxl — only required if you want styled .xlsx output
#              (auto-detected when --output ends in .xlsx)
#
# Amazon Linux package manager install (recommended on EC2):
#   AL2023:  sudo dnf install -y python3-boto3 python3-openpyxl
#   AL2:     sudo yum install -y python3-boto3 python3-openpyxl
#
# pip install:
#   pip3 install -r requirements.txt
boto3>=1.26.0
openpyxl>=3.0.0
